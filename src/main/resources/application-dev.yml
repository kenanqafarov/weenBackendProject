spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ween_dev
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  redis:
    host: localhost
    port: 6379
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:weenorganization@gmail.com}
    password: "${MAIL_PASSWORD:kkhs sgna fdfi kmyg}"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    root: DEBUG
    com.ween: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

ween:
  jwt:
    secret: dev-secret-key-min-32-characters-long-required
    access-token-expiry: 3600
    refresh-token-expiry: 604800
  aes:
    secret-key: dev-16-char-key1
  mail:
    from: ${MAIL_FROM:weenorganization@gmail.com}
  frontend:
    verify-url: ${VERIFY_EMAIL_URL:http://localhost:5000/verify-email}
    reset-password-url: ${RESET_PASSWORD_URL:http://localhost:5173/change-password}
  api-key: dev-api-key-test
  cors:
    allowed-origins: http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://localhost:5173
